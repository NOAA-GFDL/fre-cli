fre_properties:
  shared_yamls:
    - "run/layouts.yaml"
    - "run/input_datasets.yaml"
build:
  compile: "compile.yaml"
  platform: "platforms.yaml"

experiments:
  experiment_name:
    settings: "settings.yaml"
    run: ["{{ fre_properties.shared_yamls }}", "run/exp.yaml"]
    postprocess:
    analysis:
